<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.Mint"
        tools:targetApi="31">
        <activity
            android:name=".activities.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.PinCodeActivity"/>
        <activity android:name=".activities.StartingAmountActivity"/>
        <activity android:name=".activities.CreateOperationActivity"/>
        <activity android:name=".activities.ChangeOperationActivity"/>
        <activity android:name=".activities.CreateCategoryActivity"/>
        <activity android:name=".activities.OperationsActivity"/>
        <activity android:name=".activities.OperationViewActivity"/>
    </application>

</manifest>